const HeroHeader = () => {
  return (
    <header className="hero-header">
      <h1 className="hero-title">Affordable and fast vehicle inspections <span className="hero-title-accent">done right.</span></h1>
      <p className="hero-recognition">Recognized by SkillTrades BC and CARFAX</p>
    </header>
  );
};

export default HeroHeader;
